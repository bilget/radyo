<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Radio.StreamPage" Icon="list.png">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
            <Label Text="Stream" TextColor="AntiqueWhite"></Label>
            <ListView x:Name="listBroadcast">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding BroadcastName}" TextColor="WhiteSmoke" Detail="{Binding BroadcasterName}" DetailColor="Aquamarine"></TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>